<li>
    {{ link.title | link_to: link.url }}
    <select>
        {% for child in link.links %}
            <option value="{{ child.url }}">{{ child.title }}</option>
        {% endfor %}
    </select>
</li>