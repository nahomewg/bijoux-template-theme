{% for product in collections['frontpage'].products %}
    {{ product.title | link_to: product.url }}
{% endfor %}
<div>
    {% render 'large-tile', title: 'Shop Our New Arrivals', buttonTitle: 'Shop Now', imgName: 'hero-banner-1-image.webp' %}
</div>